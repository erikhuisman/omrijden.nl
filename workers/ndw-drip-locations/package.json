{
  "name": "ndw-drip-locations",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "better-sqlite3": "^8.3.0",
    "typescript": "^5.0.4",
    "wrangler": "2.19.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev --local --persist",
    "deploy": "wrangler publish",
    "wrangler": "wrangler",
    "schema": "wrangler d1 execute drips --file=./schema.sql --local",
    "schema:deploy": "wrangler d1 execute drips --file=./schema.sql"
  },
  "dependencies": {
    "@omrijden/simplify": "*",
    "@omrijden/xml-node-stream": "*",
    "workers-qb": "^0.1.7"
  }
}
